let input = Number(prompt("nhap so ban can muon kiem tra vao day : "));
if (input % 3 === 0 && input % 5 === 0) {
    alert("so nay chia het cho ca 3 va 5 ");
} else {
    alert(" so nay khong the chia het cho ca 3 va 5 ");
}